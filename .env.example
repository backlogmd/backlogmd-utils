# Copy to .env and fill in values.
# Used by backlogmd-serve when running the dev server.

# OpenAI API key for the in-server chat agent (POST /api/chat).
# Optional: if unset, /api/chat returns a stub response.
GITHUB_WORKTREE_ENABLED=false

OPENAI_API_KEY=
OPENCODE_PLANNER_MODEL=
